import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.mide.ionic.uikomponenten',
  appName: 'WeitereUiKomponenten',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
